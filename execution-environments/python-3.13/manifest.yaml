identifier: python-3.13
name: Python 3.13
runtime: python-3.13
description: "Python 3.13 (slim) with NumPy and Pandas"
status: active
# Institution-specified runtime image. During development, build locally from
# the Dockerfile in this directory. In production, build and publish to a
# registry chosen by the institution.
image_reference: "epibridge/python-3.13:latest"
validation:
  setup_command: >
    if [ -f /analysis/requirements.txt ]; then
      pip install --no-cache-dir -r /analysis/requirements.txt;
    fi
