# EpiBridge Data Resource Manifest
#
# The canonical representation of institutional data resources.
# The database is a runtime index; the manifest is the source of truth.
#
# Required fields per entry:
#   identifier, name, alias, provider, endpoint

resources:
  - identifier: demo-surveillance
    name: Demonstration Surveillance Dataset
    alias: demo_surveillance
    provider: csv
    endpoint:
      path: demo_surveillance
    description: "Synthetic surveillance dataset for demonstration, testing and documentation purposes only"
    version: "1.0.0"
    status: active
